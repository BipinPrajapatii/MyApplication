@{ 
    Layout = "~/Views/Shared/_Account.cshtml";
    ViewBag.Title = "Forgot Password";
}
@model MyApplication.Web.Models.ForgotPasswordViewModel
<div class="card">
    <div class="card-body">

        <h3 class="text-center mt-0 m-b-15">
            <a href="index.html" class="logo logo-admin"><img src="~/images/logo.png" height="30" alt="logo"></a>
        </h3>

        <h4 class="text-muted text-center font-18"><b>Reset Password</b></h4>

        <div class="p-3">
            <form class="form-horizontal m-t-20" asp-action="ForgotPassword" asp-controller="Account" method="post">
                
                @{await Html.RenderPartialAsync("~/Views/Shared/_ShowAlert1.cshtml");}

                <div class="form-group">
                    <div class="col-xs-12">
                        <input class="form-control" type="email" asp-for="Email" placeholder="Email">
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "error" })
                    </div>
                </div>

                <div class="form-group text-center row m-t-20">
                    <div class="col-12">
                        <button class="btn btn-info btn-block waves-effect waves-light" type="submit">Send Email</button>
                    </div>
                </div>
                <div class="form-group m-t-10 mb-0 row">
                    <div class="col-sm-7 m-t-20">
                        <a href="/Account/Login" class="text-muted"><i class="mdi mdi-lock"></i> Back to login</a>
                    </div>
                </div>

            </form>
        </div>

    </div>
</div>
